# Changelog

## v1.1.0 - 2026-02-02
- Removed GitHub CLI (`gh`) dependency from `/merge` command - now uses standard git only
- Added note to README clarifying that GitHub CLI is not required

## v1.0.0 - Initial Release
- Core slash commands: `/push`, `/merge`, `/status`, `/log`, `/catchup`, `/audit`
- Worktree commands: `/setup-worktree`, `/fix-worktree`, `/new-feature`
- `/setup-slash-commands` for easy cross-platform installation
